# Warehouse Management System Assignment

This assignment focuses on working with a warehouse dataset and answering various questions related to warehouse operations using SQL queries.

## Assignment Questions

1. Select all warehouses.
   - Write a query to retrieve information about all warehouses. 

2. Select all boxes with a value larger than $150.
   - Write a query to find boxes with a value greater than $150. 

3. Select all distinct contents in all the boxes.
   - Write a query to retrieve the unique contents present in all boxes.    

4. Select the average value of all the boxes.
   - Write a query to calculate the average value of all boxes.

5. Select the warehouse code and the average value of the boxes in each warehouse.
   - Write a query to get the average value of boxes for each warehouse.

6. Select only those warehouses where the average value of the boxes is greater than 150.
   - Write a query to find warehouses with an average box value greater than 150.

7. Select the code of each box, along with the name of the city the box is located in.
   - Write a query to combine box codes with their corresponding city names.

8. Select the warehouse codes, along with the number of boxes in each warehouse.
   - Write a query to count the number of boxes in each warehouse.

9. Optionally, take into account that some warehouses are empty.
   - Write a query to show all warehouses, including those with zero boxes. In our data, there is no box with zeros so table would not have any zeros mentioned. 

10. Select the codes of all warehouses that are saturated.
    - Write a query to find warehouses where the number of boxes exceeds the capacity.

11. Select the codes of all the boxes located in Chicago.
    - Write a query to retrieve box codes from boxes located in Chicago.

12. Create a new warehouse in New York with a capacity for 3 boxes.
    - Write an SQL statement to insert a new warehouse record.

13. Create a new box with specific details.
    - Write an SQL statement to insert a new box with with code "H5RT", containing "Papers" with a value of $200,and located in warehouse 2.

14. Reduce the value of all boxes by 15%.
    - Write an SQL statement to update box values by applying a reduction.

15. Remove all boxes with a value lower than $100.
    - Write an SQL statement to delete boxes with a value less than $100.

16. Remove all boxes from saturated warehouses.
    - Write an SQL statement to delete boxes from warehouses exceeding capacity.

17. Add an Index for the "Warehouse" column in the "boxes" table.
    - Write an SQL statement to create an index on the "Warehouse" column. In this the index was named indexwarehouse for the purpose of it. 

18. Print all the existing indexes.
    - Write an SQL query to display existing indexes on tables. The previous menntioned index should also show up in the existing indexes alongside others
      as the index is added previous to raising this query. 

19. Remove (drop) the index you added.
    - Write an SQL statement to remove the previously added index.

